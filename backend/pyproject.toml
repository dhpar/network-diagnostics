[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "D", email = "davhern@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "netifaces2>=0.0.22",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
